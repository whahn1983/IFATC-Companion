org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

android.useAndroidX=true
android.nonTransitiveRClass=true
android.enableR8.fullMode=true

kotlin.code.style=official

# Release signing is supplied out-of-band (never commit keystores or passwords).
# Provide these in ~/.gradle/gradle.properties or as environment variables:
#   IFATC_RELEASE_STORE_FILE, IFATC_RELEASE_STORE_PASSWORD,
#   IFATC_RELEASE_KEY_ALIAS, IFATC_RELEASE_KEY_PASSWORD
